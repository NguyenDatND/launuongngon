{
  "name": "launuongngon",
  "private": true,
  "scripts": {
    "dev": "concurrently \"pnpm --filter backend dev\"",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
